{
  "name": "juego",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "httpserver --port 8000"
  },
  "dependencies": {
    "phaser": "^3.17.0"
  },
  "devDependencies": {
    "httpserver": "^0.3.0"
  }
}
